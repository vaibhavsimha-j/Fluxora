name: Keep Alive

on:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Fluxora
        run: curl -s -o /dev/null https://fluxora.streamlit.app
