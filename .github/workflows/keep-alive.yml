name: Keep Alive

on:
  schedule:
    - cron: '*/20 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Fluxora
        run: curl -A "Mozilla/5.0" -L "https://fluxora.streamlit.app" -o /dev/null -s || true
